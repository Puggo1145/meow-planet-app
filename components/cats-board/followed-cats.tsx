import { View, ScrollView, Text } from "react-native";

const FollowedCats: React.FC = () => {
    return (
        <View>
            <ScrollView>
                <Text>
                    Followed Cats
                </Text>
            </ScrollView>
        </View>
    );
};

export default FollowedCats;